sentry:
  dsn: ${VITE_SENTRY_DSN}
  environment: production
  tracesSampleRate: 0.1
  
analytics:
  google:
    measurementId: ${GA_MEASUREMENT_ID}
  plausible:
    domain: ${PLAUSIBLE_DOMAIN}

alerts:
  email: alerts@salespro.com
  slack: ${SLACK_WEBHOOK}
  pagerduty: ${PAGERDUTY_KEY}
