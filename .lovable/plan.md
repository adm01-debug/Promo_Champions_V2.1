
User quer Next Step Suggestions contextualizadas avançadas. Já existe básico (`NextBestActionCard`, `next-best-action` function, `useNextBestAction`). Preciso ver o que existe pra propor evolução.

Vou explorar rapidamente.
