# 🔍 Auditoria Enterprise — 2026-07-13

**Executor:** Claude Opus 4.8 (multi-agent — Code Auditor + Supabase Engineer + UI Architect)
**Escopo:** 100% do repositório (`src/`, `supabase/functions/`, infra, CI/CD)
**Método:** análise estática (tsgo, ripgrep, wc), inspeção de padrões, cruzamento com memória do projeto.

---

## 1. Sumário Executivo

| Dimensão        | Score  | Observação                                                                 |
| --------------- | ------ | -------------------------------------------------------------------------- |
| TypeScript      | 8.5/10 | `tsgo --noEmit` limpo; **232 ocorrências de `any`** ainda espalhadas.       |
| Lint            | 6/10   | `npm run lint` quebrando com `ERR_INTERNAL_ASSERTION` do Node (bloqueia CI).|
| Arquitetura     | 8/10   | **46 arquivos > 400 linhas** violam a regra "helpers extraction" do memory.|
| Segurança       | 9/10   | RLS/GRANTs consistentes; edge functions com `withRequestId` + retry + CB.  |
| Observabilidade | 9.5/10 | Telemetria de retries, circuit breaker, cron monitors, threshold alerts.   |
| Testes          | 9/10   | Fuzzing + Deno tests + Playwright E2E abrangentes.                         |
| Dependências    | 7/10   | ~60 pacotes desatualizados (radix minor, tanstack minor, supabase-js).     |
| **GLOBAL**      | **8.4/10** | Já é enterprise-grade. Gaps concentrados em higiene de código.         |

---

## 2. Falhas & Lacunas Priorizadas

### 🔴 P0 — Bloqueadores de CI/qualidade

| ID     | Achado                                                                                                     | Arquivo                                    | Impacto                     |
| ------ | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------ | --------------------------- |
| CI-01  | `npm run lint` falha com `ERR_INTERNAL_ASSERTION` (eslint 10.5 + Node ESM CJS bridge quebrado).            | `eslint.config.js` + Node runtime          | CI quality gate cego.       |
| TS-01  | 232 usos de `: any` / `as any` / `<any>` em 111 arquivos — viola "Strict TS Policy" (mem).                 | `src/**` (top: recharts formatters, hooks) | Perda de type safety.       |

### 🟡 P1 — Débito arquitetural

| ID     | Achado                                                                                                          | Evidência                                                                                            |
| ------ | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| ARCH-01| **46 arquivos > 400 linhas** violando `mem://architecture/modularization-and-edge-standards`.                    | `LeadScoringDashboard.tsx` 1061, `RaceArena.tsx` 982, `Client360View.tsx` 943, `Speedometer.tsx` 878.|
| ARCH-02| 70 diretivas `eslint-disable-next-line react-hooks/exhaustive-deps` com comentário genérico "pre-existente".    | Concentrado em `race/*`, `security/*`, `lead-scoring/*`.                                             |
| ARCH-03| `src/integrations/supabase/types.ts` = 21.573 linhas (auto-gen, ok, mas nunca purgado de tabelas removidas).    | Peso no TS server; possível causa do lint crash.                                                     |

### 🔵 P2 — Higiene contínua

| ID     | Achado                                                                                                          |
| ------ | --------------------------------------------------------------------------------------------------------------- |
| DEP-01 | ~60 pacotes desatualizados. Radix minor bumps, `@tanstack/react-query` 5.83→5.101, `@supabase/supabase-js` 2.108→2.110.|
| DEP-02 | `@types/react` fixado em 18.3 (correto, bloqueio consciente por `react-leaflet`). Documentar em ADR.            |
| DOC-01 | `TEST_QUALITY_REPORT.md` na raiz duplica `docs/reports/TEST_QUALITY_REPORT.md`.                                  |
| SEC-01 | 0 findings críticos ativos no scanner. Manter cron `edge-retry-threshold-alert-15min` + `wal-health-alert`.     |

---

## 3. Backlog de Execução (ordenado por ROI)

1. **CI-01** — corrigir crash do eslint (downgrade eslint 9.x ou trocar loader ESM, ou usar `eslint --flag unstable_config_lookup_from_file`).
2. **TS-01** — mutirão de tipagem: substituir `formatter: any` do Recharts pelo tipo utilitário `RechartsFormatter` já existente em `src/types/recharts.ts`. Alvo: -80% de `any` em componentes de dashboard.
3. **ARCH-01** — extrair helpers dos 5 arquivos gigantes prioritários (`LeadScoringDashboard`, `RaceArena`, `Client360View`, `Speedometer`, `PricingIntelligenceHub`) para `*Helpers.ts` e sub-componentes < 300 linhas.
4. **ARCH-02** — auditar cada `eslint-disable exhaustive-deps`: converter para `useEffectEvent` (React 18.3) ou `useRef` estável, ou justificar em comentário técnico específico (não boilerplate).
5. **DEP-01** — bump minor coordenado (radix + tanstack + supabase-js) em uma única PR, com `npm run build && npm run test`.
6. **DOC-01** — remover `TEST_QUALITY_REPORT.md` raiz; regenerar único em `docs/reports/`.

---

## 4. Ausência de "Gambiarra"

Amostragem semântica (`TODO|FIXME|HACK|gambiarra`) → **0 matches**. O código já não carrega placeholders de débito explícito. Os pontos frágeis remanescentes são **implícitos**: os `eslint-disable` genéricos (ARCH-02) e os `any` (TS-01) são as verdadeiras "gambiarras silenciosas".

---

## 5. Recomendação Final

O sistema está em **8.4/10 enterprise-grade**. Para atingir **10/10** basta executar P0+P1 (~2 dias de trabalho focado). Não há reescrita necessária, apenas **higiene disciplinada**.

Próxima ação executada nesta sessão: **CI-01** (destravar eslint) — ver commit seguinte.

---
*Auditoria gerada com base em tsgo, ripgrep, npm outdated, security scanner e memória do projeto.*
