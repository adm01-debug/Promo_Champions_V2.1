export function Drawer({open, onClose, children, position = 'right'}: any) { if (!open) return null; const pos = position === 'right' ? 'right-0' : 'left-0'; return <div className="fixed inset-0 z-50"><div className="absolute inset-0 bg-black/50" onClick={onClose} /><div className={`absolute top-0 ${pos} h-full w-96 bg-white shadow-xl transform transition-transform`}>{children}</div></div>; }