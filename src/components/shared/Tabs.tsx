import { useState } from 'react'; export function Tabs({tabs}: any) { const [active, setActive] = useState(0); return <div><div className="flex border-b">{tabs.map((tab: any, i: number) => <button key={i} onClick={() => setActive(i)} className={`px-4 py-2 ${active === i ? 'border-b-2 border-blue-500 font-bold' : 'text-gray-600'}`}>{tab.label}</button>)}</div><div className="p-4">{tabs[active]?.content}</div></div>; }