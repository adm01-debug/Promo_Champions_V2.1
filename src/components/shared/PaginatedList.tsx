import { useState } from 'react'; export function PaginatedList({items, perPage = 10}: any) { const [page, setPage] = useState(0); const start = page * perPage; const paginated = items.slice(start, start + perPage); const total = Math.ceil(items.length / perPage); return <div><div className="space-y-2 mb-4">{paginated.map((i: any) => <div key={i.id} className="border rounded p-3">{i.title}</div>)}</div><div className="flex justify-between"><button onClick={() => setPage(p => Math.max(0, p - 1))} disabled={page === 0} className="px-4 py-2 border rounded">Anterior</button><span>Página {page + 1} de {total}</span><button onClick={() => setPage(p => Math.min(total - 1, p + 1))} disabled={page >= total - 1} className="px-4 py-2 border rounded">Próxima</button></div></div>; }