export function DataGrid({data, cols}: any) { return <div className="overflow-x-auto"><table className="w-full"><thead><tr>{cols.map((c: any) => <th key={c.key} className="text-left p-2 border-b">{c.label}</th>)}</tr></thead><tbody>{data.map((r: any, i: number) => <tr key={i} className="hover:bg-gray-50">{cols.map((c: any) => <td key={c.key} className="p-2 border-b">{r[c.key]}</td>)}</tr>)}</tbody></table></div>; }