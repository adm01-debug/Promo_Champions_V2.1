import { useState, useRef, useEffect } from 'react'; export function Popover({trigger, content}: any) { const [open, setOpen] = useState(false); const ref = useRef<HTMLDivElement>(null); useEffect(() => { const handleClick = (e: any) => { if (ref.current && !ref.current.contains(e.target)) setOpen(false); }; document.addEventListener('click', handleClick); return () => document.removeEventListener('click', handleClick); }, []); return <div ref={ref} className="relative"><div onClick={() => setOpen(!open)}>{trigger}</div>{open && <div className="absolute z-10 mt-2 bg-white border rounded-lg shadow-lg p-4">{content}</div>}</div>; }