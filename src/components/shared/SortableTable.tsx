import { useState } from 'react'; export function SortableTable({data, cols}: any) { const [sort, setSort] = useState({key: '', dir: 'asc'}); const sorted = [...data].sort((a, b) => { if (!sort.key) return 0; return sort.dir === 'asc' ? a[sort.key] > b[sort.key] ? 1 : -1 : a[sort.key] < b[sort.key] ? 1 : -1; }); return <DataGrid data={sorted} cols={cols.map((c: any) => ({...c, label: <span className="cursor-pointer" onClick={() => setSort({key: c.key, dir: sort.key === c.key && sort.dir === 'asc' ? 'desc' : 'asc'})}>{c.label} {sort.key === c.key && (sort.dir === 'asc' ? '↑' : '↓')}</span>}))} />; }