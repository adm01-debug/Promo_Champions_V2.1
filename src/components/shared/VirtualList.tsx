import { useRef, useState, useEffect } from 'react'; export function VirtualList({items, height = 500, itemHeight = 50}: any) { const [scroll, setScroll] = useState(0); const start = Math.floor(scroll / itemHeight); const end = Math.min(items.length, start + Math.ceil(height / itemHeight) + 1); const visible = items.slice(start, end); return <div style={{height, overflow: 'auto'}} onScroll={e => setScroll(e.currentTarget.scrollTop)}><div style={{height: items.length * itemHeight, position: 'relative'}}><div style={{position: 'absolute', top: start * itemHeight, width: '100%'}}>{visible.map((i: any, idx: number) => <div key={start + idx} style={{height: itemHeight}} className="border-b p-2">{i.title}</div>)}</div></div></div>; }