export function Breadcrumb({items}: any) { return <nav className="flex"><ol className="flex items-center space-x-2">{items.map((item: any, i: number) => <li key={i} className="flex items-center">{i > 0 && <span className="mx-2 text-gray-400">/</span>}{item.href ? <a href={item.href} className="text-blue-500 hover:underline">{item.label}</a> : <span className="text-gray-600">{item.label}</span>}</li>)}</ol></nav>; }