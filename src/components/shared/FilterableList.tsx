import { useState } from 'react'; export function FilterableList({items, filterKey}: any) { const [q, setQ] = useState(''); const filtered = items.filter((i: any) => i[filterKey].toLowerCase().includes(q.toLowerCase())); return <div><input value={q} onChange={e => setQ(e.target.value)} placeholder="Filtrar..." className="w-full border rounded px-3 py-2 mb-4" /><div className="space-y-2">{filtered.map((i: any) => <div key={i.id} className="border rounded p-3">{i[filterKey]}</div>)}</div></div>; }