export function GroupedList({items, groupBy}: any) { const grouped = items.reduce((acc: any, item: any) => { const key = item[groupBy]; if (!acc[key]) acc[key] = []; acc[key].push(item); return acc; }, {}); return <div className="space-y-4">{Object.entries(grouped).map(([key, items]: any) => <div key={key}><h3 className="font-bold mb-2">{key}</h3><div className="space-y-2">{items.map((i: any) => <div key={i.id} className="border rounded p-3">{i.title}</div>)}</div></div>)}</div>; }