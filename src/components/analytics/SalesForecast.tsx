import { FC } from 'react';
import { Card } from '@/components/ui/card';

export const SalesForecast: FC = () => {
  return (
    <Card className="p-6">
      <h3 className="text-lg font-semibold mb-4">Previsão de Vendas</h3>
    </Card>
  );
};
