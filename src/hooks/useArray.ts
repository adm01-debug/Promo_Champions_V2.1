import { useState } from 'react'; export function useArray<T>(initial: T[]) { const [arr, setArr] = useState(initial); return { arr, set: setArr, push: (e: T) => setArr(a => [...a, e]), filter: (cb: (e: T) => boolean) => setArr(a => a.filter(cb)), update: (i: number, n: T) => setArr(a => [...a.slice(0, i), n, ...a.slice(i + 1)]), remove: (i: number) => setArr(a => [...a.slice(0, i), ...a.slice(i + 1)]), clear: () => setArr([]) }; }