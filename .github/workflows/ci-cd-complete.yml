name: Complete CI/CD Pipeline

# Workflow desabilitado temporariamente até testes serem recriados
# Será reativado após Issue #25

on:
  workflow_dispatch:  # Manual apenas

env:
  CI: true
  NODE_VERSION: '20'

jobs:
  info:
    name: Pipeline Info
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "Pipeline temporariamente desabilitado"
          echo "Aguardando recriação de testes (Issue #25)"
          echo "Use 'pr-checks.yml' para validação de PRs"
