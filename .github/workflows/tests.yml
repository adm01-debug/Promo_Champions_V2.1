name: Tests

# Workflow desabilitado temporariamente - testes foram removidos
# Será reativado quando os testes forem recriados (Issue #25)

on:
  workflow_dispatch:  # Manual apenas

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: echo "Tests temporariamente desabilitados - Issue #25"
      # - run: npm test  # Desabilitado - testes removidos
