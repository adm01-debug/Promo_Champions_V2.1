/**
 * Thin shim — the canonical fixtures live in `src/lib/winloss/atRiskFixtures.ts`
 * so they can be imported from both the Vite bundle (UI) and Deno tests.
 *
 * Do NOT add new exports here; edit the canonical module instead.
 */
export * from "../../../src/lib/winloss/atRiskFixtures.ts";
