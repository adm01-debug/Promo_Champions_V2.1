# Dependencies
node_modules/
.pnp
.pnp.js

# Build
dist/
build/
.vite/

# Environment (nunca commitar nenhuma variante; .env.example é a exceção)
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
coverage/
.lighthouseci/
test-results/
playwright-report/

# Logs
*.log
npm-debug.log*

# Runtime — lockfile canônico é package-lock.json (CI usa npm ci)
bun.lockb
bun.lock

# Estado efêmero gerado pela Supabase CLI (link, versões e cache local)
supabase/.temp/

# E2E session cache
tests/e2e/.auth/

# Onda O — bundle analyzer output
bundle-stats/
dependency-graph.svg

# Onda Q — a11y sweep artifacts
a11y-report.json
a11y-report-html/
graphify-out/graph.json
graphify-out/graph.html

# Snapshots locais do Graphify — nunca entram no índice nem em artefatos de CI.
.graphify-local/
graphify-out/cache/
**/graphify-out/cache/
