legacy-peer-deps=true
# Rewrite any Lovable/GCP private registry URLs in the lock file to public npm.
# Without this, CI environments that lack Lovable registry auth fail on npm ci.
replace-registry-host=npmjs
